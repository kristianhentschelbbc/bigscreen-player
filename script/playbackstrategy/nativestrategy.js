
import LegacyAdapter from "./legacyplayeradapter";
import WindowTypes from "../models/windowtypes";

export default function NativeStrategy (mediaSources, windowType, mediaKind, playbackElement, isUHD) {
  pickStrategy();

  function pickStrategy() {

  }

    function init(mediaPlayer) {

    }

    return NativeStrategy;
};

